# Unverified endpoints

Requests in this directory are preserved for local investigation but are outside
the authoritative HCM OpenAPI contract. The API inventory generator excludes
this tree, so these requests do not produce active generated pytest checks and
are not invoked by HCM CI.

Move a request back into the verified Bruno tree only after its endpoint is
added to the authoritative contract and approved for HCM test scope.
