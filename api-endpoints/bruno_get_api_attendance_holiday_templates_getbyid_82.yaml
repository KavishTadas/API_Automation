# AUTHORED -- this file is the source of truth for this endpoint.
#
# A `cases[]` entry is one test case and carries all 18 inventory columns.
# Content lives per case, not per endpoint: on POST /auth/token the purpose,
# request payload and response differ across tc01/tc02/tc03, and folding them
# up to the endpoint silently discarded two of the three.
#
# canonicalRef is contract-visible. Do not normalise it, do not fix its
# spelling -- the harness, run manifest and result document all key on it.
slug: bruno_get_api_attendance_holiday_templates_getbyid_82
module: Bruno
method: GET
endpointPath: /api/attendance/holiday-templates/getById/82
environments:
- UAT
version: null
credentialAlias: null
metadata: null
cases:
- apiId: '46'
  module: Bruno
  subModule: Holiday Template By Id 82
  access: private
  purpose: Status is 200
  basePath: '{{attendanceBaseUrl}}'
  endpointPath: /api/attendance/holiday-templates/getById/82
  method: GET
  requestParameters: 'auth: bearer token={{authToken}}'
  requestBodySchema: null
  responseExample: 'Expected status(es): 200'
  owner: Parth Divekar <parthdivekar987@gmail.com>
  canonicalRef: get|/api/attendance/holiday-templates/getbyid/82|bruno|holiday template by id 82
  samplePayloads: []
  rules:
  - category: Dependency
    description: Requires authToken from Employee Auth API; Depends on Employee Auth API POST /auth/token
      for authToken
  - category: Non-Functional
    description: 'Source: bruno/Holiday Template By Id 82.bru; No saved response example in source file;
      Parsed from Bruno file'
  assertionState: asserted-not-executed
  sourceType: bruno
  sourceCollection: bruno/Holiday Template By Id 82.bru
