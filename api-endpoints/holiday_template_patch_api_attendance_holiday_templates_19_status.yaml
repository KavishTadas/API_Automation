# AUTHORED -- this file is the source of truth for this endpoint.
#
# A `cases[]` entry is one test case and carries all 18 inventory columns.
# Content lives per case, not per endpoint: on POST /auth/token the purpose,
# request payload and response differ across tc01/tc02/tc03, and folding them
# up to the endpoint silently discarded two of the three.
#
# canonicalRef is contract-visible. Do not normalise it, do not fix its
# spelling -- the harness, run manifest and result document all key on it.
slug: holiday_template_patch_api_attendance_holiday_templates_19_status
module: Holiday Template APIs Copy
method: PATCH
endpointPath: /api/attendance/holiday-templates/19/status
environments:
- UAT
version: null
credentialAlias: null
metadata: null
cases:
- apiId: '29'
  module: Holiday Template APIs Copy
  subModule: Change Status
  access: private
  purpose: Change Status
  basePath: '{{baseURL}}'
  endpointPath: /api/attendance/holiday-templates/19/status
  method: PATCH
  requestParameters: 'headers: Authorization=Bearer {{jwtToken}}; Content-Type=application/json'
  requestBodySchema: '{"isActive":"string"}'
  responseExample: null
  owner: KavishTadas <kavish.tadas@omfysgroup.com>
  canonicalRef: patch|/api/attendance/holiday-templates/19/status|holiday template apis copy|change status
  samplePayloads:
  - payloadType: Request Body
    json: |-
      {
        "isActive": "N"
      }
  - payloadType: Example Request Payload
    json: |-
      {
        "isActive": "N"
      }
  rules:
  - category: Non-Functional
    description: 'Source: collections/Holiday_Template_API.json; No saved response example in source file'
  assertionState: not-asserted
  sourceType: newman
  sourceCollection: collections/Holiday_Template_API.json
