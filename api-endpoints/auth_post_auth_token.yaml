# GENERATED by scripts/generate-endpoint-yaml.py for Phase 2.
# From Phase 3 this file becomes the source of truth and is edited
# by hand. canonicalRef is contract-visible -- do not normalise it.
slug: auth_post_auth_token
apiId: '1'
name: Login
module: auth
subModule: Login
purpose: Status is 200; Token exists; empCode matches
owner: KavishTadas <kavish.tadas@omfysgroup.com>
method: POST
basePath: '{{baseUrl}}'
endpointPath: /auth/token
authType: public
environments:
- UAT
version: null
canonicalRef: post|/auth/token|auth|login
credentialAlias: null
metadata:
  x-required-role: none
  x-sla-ms: 1000
samplePayloads:
- payloadType: Request Body
  responseStatus: null
  json: |
    {
      "empCode": "<empCode>",
      "password": "<empPassword>"
    }
- payloadType: Success Response
  responseStatus: 200
  json: |
    {
      "token": "<jwt-token>"
    }
- payloadType: Request Body
  responseStatus: null
  json: |
    {
        "empCode": "{{empCode}}",
        "password": "{{empPassword}}"
      }
rules:
- category: Business Rule
  description: Status is 200; Token exists; empCode matches
- category: Dependency
  description: Produces authToken for downstream APIs
- category: Non-Functional
  description: 'Source: bruno/auth/login.bru; No saved response example in source file; Parsed from Bruno
    file'
testCases:
- canonicalRef: post|/auth/token|auth|login
  name: Login
  assertionState: asserted-not-executed
  sourceType: bruno
  sourceCollection: bruno/auth/login.bru
