# cURL command matrix

Generated by `scripts/generate_curl_matrix.py` from every source present:
the inventory, the Postman collections, an OpenCollection export if one is
cloned, and the spec workbook.

**No credential appears below.** `Authorization` is emitted as `Bearer $TOKEN`,
and credential fields inside request bodies -- an employee code, a password --
are replaced the same way. Populate them before running:

```sh
TOKEN=$(curl -sS -X POST 'https://uat-mcdp-be.omfysgroup.com/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"<EMP_CODE>","password":"<PASSWORD>"}' | jq -r '.token')
```

## (unattributed)

### DELETE /api/attendance/holiday-templates/delete/{holidayTemplateId}

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendance/holiday-templates/delete/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PATCH /api/attendance/week-off-assignment/changeStatus

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"assignmentId":3,"status":"N"}'
```

### POST /api/attendance/week-off-assignment/getByWeekOff

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffId":1}'
```

### PATCH /api/attendance/week-offs/actDeact/3/status

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"status":"N"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/week-offs/actDeact/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

### POST /api/attendance/week-offs/create

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### PUT /api/attendance/week-offs/update/1

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/week-offs/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### GET /api/v1/attendance/shift/master

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer not-a-real-token'
```

### POST /api/v1/attendance/shift/master

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Content-Type: application/json' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

### PUT /api/v1/attendance/shift/master/2

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseURL}}/api/v1/attendance/shift/master/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

### DELETE /api/v1/attendance/shift/master/6

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseURL}}/api/v1/attendance/shift/master/6' \
  -H 'Authorization: Bearer $TOKEN' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseURL}}/api/v1/attendance/shift/master/6' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseURL}}/api/v1/attendance/shift/master/6' \
  -H 'Authorization: Bearer not-a-real-token' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseURL}}/api/v1/attendance/shift/master/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

### GET /api/v1/attendance/shift/master/7

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master/7' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master/7'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master/7' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseURL}}/api/v1/attendance/shift/master/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PATCH /api/v1/attendance/shift/master/status/7

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/v1/attendance/shift/master/status/7' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/v1/attendance/shift/master/status/7'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/v1/attendance/shift/master/status/7' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/v1/attendance/shift/master/status/7' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseURL}}/api/v1/attendance/shift/master/status/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET /user/leaves/getAllLeaveReports

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{leaveBaseUrl}}/user/leaves/getAllLeaveReports' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{leaveBaseUrl}}/user/leaves/getAllLeaveReports'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{leaveBaseUrl}}/user/leaves/getAllLeaveReports' \
  -H 'Authorization: Bearer not-a-real-token'
```

### GET /users/me

<sub>source: inventory</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{BASE_URL}}/users/me'
```

## Attendance Management

### GET {{baseURL}}/api/attendancepolicy

*Get All Policies*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendancepolicy'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendancepolicy'
```

### POST {{baseURL}}/api/attendancepolicy

*Create New Policy*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendancepolicy' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendancepolicy' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendancepolicy' \
  -H 'Content-Type: text/plain' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendancepolicy' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

### GET {{baseURL}}/api/attendancepolicy/1

*Get Policy By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendancepolicy/1'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendancepolicy/999999'
```

### DELETE {{baseURL}}/api/attendancepolicy/2

*Delete Policy By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseURL}}/api/attendancepolicy/2'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseUrl}}999999}/api/attendancepolicy/999999'
```

### PUT {{baseURL}}/api/attendancepolicy/2

*Update Policy By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendancepolicy/2' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendancepolicy/2' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendancepolicy/2' \
  -H 'Content-Type: text/plain' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/attendancepolicy/999999' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

### PATCH {{baseURL}}/api/attendancepolicy/4/status

*Activate Deactivate*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendancepolicy/4/status'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendancepolicy/4/status' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendancepolicy/999999/status'
```

### GET {{baseURL}}/api/v1/attendance/shift/master

*Get All Shifts*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/v1/attendance/shift/master'
```

### POST {{baseURL}}/api/v1/attendance/shift/master

*Create New Shift*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Content-Type: application/json' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master' \
  -H 'Content-Type: text/plain' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/v1/attendance/shift/master' \
  -H 'Content-Type: application/json' \
  -d '{"shiftCode":"SHF-MIDNIGHT-001","shiftName":"MidNight Shift - Standard","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"09:00:00","endTime":"18:00:00","graceMinutes":15,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Dec-2026","isDeleted":"N","description":"xyz","remarks":"Standard morning shift for all employees","breakConfigs":[{"breakName":"Lunch Break","breakDurationMinutes":60,"paidFlag":false},{"breakName":"Tea Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Evening Tea","breakDurationMinutes":15,"paidFlag":true}],"applicableEmpIds":null}'
```

### PUT {{baseURL}}/api/v1/attendance/shift/master/2

*Update Shift*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/2' \
  -H 'Content-Type: text/plain' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/v1/attendance/shift/master/999999' \
  -H 'Content-Type: application/json' \
  -d '{"shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"description":"xyz","remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

### DELETE {{baseURL}}/api/v1/attendance/shift/master/6

*Delete Shift*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/6' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseUrl}}999999}/api/v1/attendance/shift/master/999999' \
  -d '{"shiftCode":"SHF-NIGHT-0002","shiftName":"Night Shift -Techno Support Team","shiftType":"FIXED","applicabilityType":"DEFAULT","startTime":"22:00:00","endTime":"06:00:00","graceMinutes":10,"requiredDurationHours":null,"effectiveFrom":"01-Aug-2026","effectiveTo":"31-Mar-2027","activeFlag":true,"remarks":"Night shift for global support team (cross-midnight)","breakConfigs":[{"breakName":"Dinner Break","breakDurationMinutes":45,"paidFlag":false},{"breakName":"Shortscut Break","breakDurationMinutes":15,"paidFlag":true},{"breakName":"Refreshment Break","breakDurationMinutes":10,"paidFlag":true}],"applicableEmpIds":[3496,3500]}'
```

### GET {{baseURL}}/api/v1/attendance/shift/master/7

*Get By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/7'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/v1/attendance/shift/master/999999'
```

### PATCH {{baseURL}}/api/v1/attendance/shift/master/status/7

*Toggle Delete*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/status/7'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/v1/attendance/shift/master/status/7' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/v1/attendance/shift/master/status/999999'
```

## Attendance Policy

### POST /api/attendancepolicy

*Create Attendance Policy*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/attendancepolicy' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyName":"Late Coming","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

### GET /api/attendancepolicy/1

*Fetch Specific Attendance Policy by ID*
<sub>source: inventory, yaml-collection · ids seen: -1, 20, 22, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy/1' \
  -H 'Content-Type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy/1' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

### DELETE /api/attendancepolicy/2

*" Delete Attendance Policy By ID"*
<sub>source: inventory, yaml-collection · ids seen: 22</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Content-Type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseURL}}/api/attendancepolicy/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

### PUT /api/attendancepolicy/2

*" Mutation on Non-Existent Entity ID"*
<sub>source: inventory, yaml-collection · ids seen: 20, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendancepolicy/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyName":"Late Coming Policy","policyHeading":"Late Coming Policy Heading","policyDescription":"Late Coming Policy Description"}'
```

### PATCH /api/attendancepolicy/4/status

*Action Enum Tampering Fuzzing Test*
<sub>source: inventory, yaml-collection · ids seen: 20</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendancepolicy/4/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'action: TAMPERED_ACTION_PROBE'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendancepolicy/4/status' \
  -H 'Content-Type: application/json' \
  -H 'action: TAMPERED_ACTION_PROBE'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendancepolicy/4/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -H 'action: TAMPERED_ACTION_PROBE'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendancepolicy/4/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'action: TAMPERED_ACTION_PROBE' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendancepolicy/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'action: TAMPERED_ACTION_PROBE'
```

### POST /auth/token

*Login*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","password":"$PASSWORD"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}/auth/token' \
  -H 'Content-Type: text/plain' \
  -d '{"empCode":"$EMP_CODE","password":"$PASSWORD"}'
```

## Attendance Status Threshold API

### PATCH {{baseUrl}}/api/attendance/status-threshold/1/status

*Change Status*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"isActive":"Y"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendance/status-threshold/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

### POST {{baseUrl}}/api/attendance/status-threshold/create

*Create Threshold*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

### GET {{baseUrl}}/api/attendance/status-threshold/getAll

*Get All*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/status-threshold/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET {{baseUrl}}/api/attendance/status-threshold/getById/8

*Get By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getById/8' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getById/8'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/getById/8' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/status-threshold/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT {{baseUrl}}/api/attendance/status-threshold/update/1

*Update Threshold*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/attendance/status-threshold/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

### DELETE {{baseUrl}}/api/attendance/status-thresholds/delete/1

*Delete Threshold*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseUrl}}/api/attendance/status-thresholds/delete/1' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseUrl}}/api/attendance/status-thresholds/delete/1'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseUrl}}/api/attendance/status-thresholds/delete/1' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseUrl}}999999}/api/attendance/status-thresholds/delete/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

## Attenedance-july2026

### PATCH {{baseURL}}/api/attendance/week-off-assignment/changeStatus

*week-off-assignment/changeStatus*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"assignmentId":3,"status":"N"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendance/week-off-assignment/changeStatus' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"assignmentId":3,"status":"N"}'
```

### POST {{baseURL}}/api/attendance/week-off-assignment/getByWeekOff

*week-off-assignment/getByWeekOffId*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffId":1}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendance/week-off-assignment/getByWeekOff' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffId":1}'
```

### PATCH {{baseURL}}/api/attendance/week-offs/actDeact/3/status

*WeekOffActDeact*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/actDeact/3/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"status":"N"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendance/week-offs/actDeact/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"status":"N"}'
```

### POST {{baseURL}}/api/attendance/week-offs/create

*WeekOffMasterCreate*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendance/week-offs/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Sunday Week Off","description":"Default Sunday Week Off","optionModel":"ALLOWED","isDefault":"Y","isActive":"Y","details":[{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":3,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":4,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":5,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-07-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### GET {{baseURL}}/api/attendance/week-offs/getAll

*WeekOffGetAll*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/week-offs/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET {{baseURL}}/api/attendance/week-offs/getById/1

*WeekOffGetById*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getById/1' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getById/1'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/getById/1' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/week-offs/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT {{baseURL}}/api/attendance/week-offs/update/1

*WeekOffUpdate*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/week-offs/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/attendance/week-offs/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"weekOffCode":"WO001","weekOffName":"Updated Sunday Week Off","description":"Updated Description","optionModel":"ALLOWED","isDefault":"N","isActive":"Y","details":[{"dayOfWeek":"SATURDAY","weekNo":2,"isWeekOff":"Y"},{"dayOfWeek":"SUNDAY","weekNo":1,"isWeekOff":"Y"}],"assignments":[{"employeeId":101,"departmentId":10,"locationId":1,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

## Employee Auth API

### POST {{authBaseUrl}}/auth/token

*TC01 - Valid credentials return JWT token*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{authBaseUrl}}/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","password":"$PASSWORD"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{authBaseUrl}}/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{authBaseUrl}}/auth/token' \
  -H 'Content-Type: text/plain' \
  -d '{"empCode":"$EMP_CODE","password":"$PASSWORD"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/auth/token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","password":"$PASSWORD"}'
```

## Global Test Cases

### GET /api/attendance/holiday-templates/1/status

*25. [GLOB-01] Toggle Holiday Status - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 1, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/holiday-templates/1/status'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/holiday-templates/999999/status'
```

### GET /api/attendance/holiday-templates/create

*1. [GLOB-01] Create Holiday Template - Missing Auth Token (401)*
<sub>source: yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/holiday-templates/create'
```

### GET /api/attendance/holiday-templates/getAll

*10. [GLOB-06] Get All Holiday Templates - Invalid Sort Injection (JPA Leak Probe)*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

### GET /api/attendance/holiday-templates/update/1

*19. [GLOB-01] Update Holiday Template - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: 1</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/holiday-templates/update/1'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/holiday-templates/update/999999'
```

### GET /api/attendance/late-early-policies/138/status

*25. [GLOB-01] Change Policy Status - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 138, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/138/status'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/999999/status'
```

### GET /api/attendance/late-early-policies/create

*1. [GLOB-01] Create Policy - Missing Auth Token (401)*
<sub>source: yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/create'
```

### GET /api/attendance/late-early-policies/update/138

*19. [GLOB-01] Update Policy - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: 138</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/update/138'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/update/999999'
```

### GET /api/attendance/status-threshold/80/status

*25. [GLOB-01] Change Threshold Status - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 80, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/80/status'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/999999/status'
```

### GET /api/attendance/status-threshold/create

*1. [GLOB-01] Create Threshold - Missing Auth Token (401)*
<sub>source: yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/create'
```

### GET /api/attendance/status-threshold/delete/80

*31. [GLOB-01] Delete Threshold - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 80, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/delete/80'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/delete/999999'
```

### GET /api/attendance/status-threshold/update/80

*19. [GLOB-01] Update Threshold - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: 80</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/update/80'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/update/999999'
```

### GET /api/attendance/week-offs/actDeact/134/status

*25. [GLOB-01] Toggle Week Off Status - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 134, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/week-offs/actDeact/134/status'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/week-offs/actDeact/999999/status'
```

### GET /api/attendance/week-offs/create

*1. [GLOB-01] Create Week Off - Missing Auth Token (401)*
<sub>source: yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/week-offs/create'
```

### GET /api/attendance/week-offs/getAll

*10. [GLOB-06] Get All Week Offs - Invalid Sort Injection (JPA Leak Probe)*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

### GET /api/attendance/week-offs/getById/1

*13. [GLOB-01] Get Week Off By ID - Missing Auth Token (401)*
<sub>source: inventory, yaml-collection · ids seen: -1, 134, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getById/1' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getById/1'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getById/1' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/week-offs/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET /api/attendance/week-offs/update/134

*19. [GLOB-01] Update Week Off - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: 134</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/week-offs/update/134'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendance/week-offs/update/999999'
```

### GET /api/attendancepolicy

*1. [GLOB-01] Create Policy - Missing Auth Token (401)*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendancepolicy' \
  -H 'Authorization: Bearer not-a-real-token'
```

### GET /api/attendancepolicy/20/status

*25. [GLOB-01] Toggle Policy Status - Missing Auth Token (401)*
<sub>source: yaml-collection · ids seen: -1, 20, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy/20/status'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET 'https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy/999999/status'
```

## HCM - Attendance

### POST /user/attendance/checkin

*Mark Check-In*
<sub>source: workbook</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkin' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkInTime":"09:00:00","location":"Office HQ"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkin' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkInTime":"09:00:00","location":"Office HQ"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkin' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkInTime":"09:00:00","location":"Office HQ"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkin' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkin' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"empCode":"$EMP_CODE","checkInTime":"09:00:00","location":"Office HQ"}'
```

### POST /user/attendance/checkout

*Mark Check-Out*
<sub>source: workbook</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkout' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkOutTime":"18:00:00","location":"Office HQ"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkout' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkOutTime":"18:00:00","location":"Office HQ"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkout' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","checkOutTime":"18:00:00","location":"Office HQ"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkout' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/checkout' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"empCode":"$EMP_CODE","checkOutTime":"18:00:00","location":"Office HQ"}'
```

### GET /user/attendance/history

*Get Attendance History*
<sub>source: workbook</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/history' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/history' \
  -H 'Content-Type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/history' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json'
```

### POST /user/attendance/regularize

*Apply Regularization*
<sub>source: workbook</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/regularize' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","date":"2026-08-04","requestedCheckIn":"09:00:00","requestedCheckOut":"18:00:00","reason":"Biometric device failure"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/regularize' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","date":"2026-08-04","requestedCheckIn":"09:00:00","requestedCheckOut":"18:00:00","reason":"Biometric device failure"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/regularize' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"empCode":"$EMP_CODE","date":"2026-08-04","requestedCheckIn":"09:00:00","requestedCheckOut":"18:00:00","reason":"Biometric device failure"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/regularize' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}/user/attendance/regularize' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"empCode":"$EMP_CODE","date":"2026-08-04","requestedCheckIn":"09:00:00","requestedCheckOut":"18:00:00","reason":"Biometric device failure"}'
```

### GET /user/attendance/summary

*Get Attendance Summary*
<sub>source: workbook</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/summary' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/summary' \
  -H 'Content-Type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}/user/attendance/summary' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json'
```

## Holiday Template APIs Copy

### PATCH {{baseURL}}/api/attendance/holiday-templates/19/status

*Change Status*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"isActive":"N"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendance/holiday-templates/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

### POST {{baseURL}}/api/attendance/holiday-templates/create

*Create*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### DELETE {{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}

*Delete*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/delete/{holidayTemplateId}' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseUrl}}999999}/api/attendance/holiday-templates/delete/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET {{baseURL}}/api/attendance/holiday-templates/getAll

*Get All*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/holiday-templates/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET {{baseURL}}/api/attendance/holiday-templates/getById/19

*Get By Id*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getById/19' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getById/19'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/getById/19' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/holiday-templates/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT {{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}

*Update*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/attendance/holiday-templates/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

## Holiday Templates

### PATCH /api/attendance/holiday-templates/19/status

*Change Holiday Template Status*
<sub>source: inventory, yaml-collection · ids seen: 126</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/19/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"isActive":"N"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/holiday-templates/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"N"}'
```

### POST /api/attendance/holiday-templates/create

*CREATE_Default_Holiday*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/holiday-templates/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"templateCode":"HOL2026","templateName":"India Holiday Calendar 2026","description":"National Holidays","effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","optionModel":"ALLOWED","templateMode":"DEFAULT","isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"2026","holidays":[{"holidayName":"Republic Day","occasion":"National Holiday","holidayDate":"2026-01-26","isOptional":"N","isHalfDay":"N","remarks":""}],"assignments":[{"employeeId":101,"effectiveFrom":"2026-01-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### GET /api/attendance/holiday-templates/getById/19

*Holiday Template_ID*
<sub>source: inventory, yaml-collection · ids seen: -1, 1, 126, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getById/19' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getById/19'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getById/19' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/holiday-templates/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT /api/attendance/holiday-templates/update/{holidayTemplateId}

*Update Holiday Template*
<sub>source: inventory, yaml-collection · ids seen: 126</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/{holidayTemplateId}' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/holiday-templates/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

## Late-Early POLICY

### PATCH /api/attendance/late-early-policies/5/status

*8. Change Policy Status (Query Param Toggle)*
<sub>source: inventory, yaml-collection · ids seen: 138</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'status: N' \
  -H 'ciontent-type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Content-Type: application/json' \
  -H 'status: N' \
  -H 'ciontent-type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -H 'status: N' \
  -H 'ciontent-type: application/json'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'status: N' \
  -H 'ciontent-type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseURL}}/api/attendance/late-early-policies/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'status: N' \
  -H 'ciontent-type: application/json'
```

### POST /api/attendance/late-early-policies/create

*6. Quotes-Only Policy Name Fuzzing Defect*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### GET /api/attendance/late-early-policies/getAll

*Unhandled 500 Entity Leak on Sort*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getAll' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -H 'page: "0"' \
  -H 'size: "10"' \
  -H 'sort: non_existent_column,desc'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getAll' \
  -H 'Content-Type: application/json' \
  -H 'page: "0"' \
  -H 'size: "10"' \
  -H 'sort: non_existent_column,desc'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getAll' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -H 'page: "0"' \
  -H 'size: "10"' \
  -H 'sort: non_existent_column,desc'
```

### GET /api/attendance/late-early-policies/getById/5

*Get Late/early policies*
<sub>source: inventory, yaml-collection · ids seen: -1, 138, 152, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getById/5' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getById/5' \
  -H 'Content-Type: application/json'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getById/5' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseURL}}/api/attendance/late-early-policies/getById/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json'
```

### PUT /api/attendance/late-early-policies/update/2

*BOLA ID Mismatch & Full Child Replacement Integrity Test*
<sub>source: inventory, yaml-collection · ids seen: 138, 152</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseURL}}/api/attendance/late-early-policies/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

## Latearly-Policy

### PATCH {{baseURL}}/api/attendance/late-early-policies/5/status

*ChangeStatus*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/5/status'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/5/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}999999}/api/attendance/late-early-policies/999999/status' \
  -H 'Authorization: Bearer $TOKEN'
```

### POST {{baseURL}}/api/attendance/late-early-policies/create

*create*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/api/attendance/late-early-policies/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":null,"policyName":"Standard Laes Ears Policises1","description":"Policy for late coming and early leaving","templateMode":"CUSTOM","eventCountMinutes":30,"graceMinutes":10,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":0.5,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"N","remarks":"Applicable for regular employees","deductionPriorities":[{"priorityOrder":1,"leaveTypeId":1,"isActive":"Y"},{"priorityOrder":2,"leaveTypeId":2,"isActive":"Y"}],"assignments":[{"employeeId":3721,"departmentId":141,"designationId":51,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isActive":"Y"}]}'
```

### GET {{baseURL}}/api/attendance/late-early-policies/getAll

*getAll*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/late-early-policies/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

### GET {{baseURL}}/api/attendance/late-early-policies/getById/5

*getById/1*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getById/5' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getById/5'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/getById/5' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/api/attendance/late-early-policies/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT {{baseURL}}/api/attendance/late-early-policies/update/2

*update*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}{{baseURL}}/api/attendance/late-early-policies/update/2' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}999999}/api/attendance/late-early-policies/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"policyCode":"LEP001","policyName":"Updated Late/Early Policy","description":"Updated Policy","templateMode":"DEFAULT","templateStatus":"PUBLISHED","eventCountMinutes":5,"graceMinutes":20,"graceEvent":2,"allowedEvent":3,"deductionType":"LEAVE","leaveDeductDays":1,"leaveTypeId":1,"fineAmount":null,"effectiveFrom":"2026-08-01","effectiveTo":null,"isDefault":"N","remarks":"Updated Policy","deductionPriorities":[{"priorityOrder":3,"leaveTypeId":1,"isActive":"Y"}],"assignments":[{"employeeId":3844,"effectiveFrom":"2026-08-01","effectiveTo":null,"isActive":"Y"}]}'
```

## Leave API

### GET {{leaveBaseUrl}}/user/leaves/getAllLeaveReports

*TC01/TC03 - Get all leave reports and validate structure*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{leaveBaseUrl}}/user/leaves/getAllLeaveReports'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/user/leaves/getAllLeaveReports'
```

## Threshold Templates

### PATCH /api/attendance/status-threshold/1/status

*PATCH Toggle Status & Activation Bypass*
<sub>source: inventory, yaml-collection · ids seen: 80</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/1/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"isActive":"Y"}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PATCH '{{baseUrl}}/api/attendance/status-threshold/999999/status' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"isActive":"Y"}'
```

### POST /api/attendance/status-threshold/create

*Competing Overlapping Threshold*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X POST '{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X POST '{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X POST '{{baseUrl}}/api/attendance/status-threshold/create' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"thresholdCode":"CUST_STD_0001","thresholdName":"Standard Custom Thresholdsl","description":"Custom threshold for flexible shift employees","templateMode":"CUSTOM","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.0,"presentMinHours":8.0,"effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31","isDefault":"N","isActive":"Y","templateStatus":"PUBLISHED","remarks":"Applicable for flexible shift employees only","assignments":[{"employeeId":41,"departmentId":1,"designationId":5,"isActive":"N","effectiveFrom":"2026-08-01","effectiveTo":"2026-12-31"},{"employeeId":550,"departmentId":1,"designationId":6,"isActive":"Y","effectiveFrom":"2026-08-01","effectiveTo":"2026-08-01"}]}'
```

### GET /api/attendance/status-threshold/getAll

*Get_threshold*
<sub>source: inventory, yaml-collection</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getAll' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getAll'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getAll' \
  -H 'Authorization: Bearer not-a-real-token'
```

### GET /api/attendance/status-threshold/getById/8

*Get Status Threshold By ID*
<sub>source: inventory, yaml-collection · ids seen: -1, 80, 999999</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getById/8' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getById/8'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getById/8' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}/api/attendance/status-threshold/getById/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

### PUT /api/attendance/status-threshold/update/1

*BOLA - ID mismatch mutation*
<sub>source: inventory, yaml-collection · ids seen: 80</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer not-a-real-token' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — wrong content type** — expect `415`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/1' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: text/plain' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X PUT '{{baseUrl}}/api/attendance/status-threshold/update/999999' \
  -H 'Authorization: Bearer $TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{"thresholdCode":"TH001","thresholdName":"Updated Threshold","description":"Updated Description","templateMode":"DEFAULT","shiftTypeApplicability":"ALL","absentMaxHours":3.0,"halfDayMinHours":3.01,"fullDayMinHours":5.01,"presentMinHours":5.02,"effectiveFrom":"2027-01-01","effectiveTo":null,"isDefault":"Y","isActive":"Y","templateStatus":"DRAFT","remarks":"Updated","assignments":[]}'
```

### DELETE /api/attendance/status-thresholds/delete/1

*DELETE Status Threshold*
<sub>source: inventory, yaml-collection · ids seen: 80</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X DELETE '{{baseUrl}}/api/attendance/status-thresholds/delete/1' \
  -H 'Authorization: Bearer $TOKEN'
```

**Negative — no credential** — expect `401 or 403`

```sh
curl -sS -X DELETE '{{baseUrl}}/api/attendance/status-thresholds/delete/1'
```

**Negative — malformed token** — expect `401`

```sh
curl -sS -X DELETE '{{baseUrl}}/api/attendance/status-thresholds/delete/1' \
  -H 'Authorization: Bearer not-a-real-token'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X DELETE '{{baseUrl}}/api/attendance/status-thresholds/delete/999999' \
  -H 'Authorization: Bearer $TOKEN'
```

## User API

### GET {{baseUrl}}/

*GET root returns health message*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/'
```

### GET {{baseUrl}}/about

*GET about returns about message*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/about'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/about'
```

### GET {{baseUrl}}/messages

*GET messages returns message collection*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X GET '{{baseUrl}}{{baseUrl}}/messages'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X GET '{{baseUrl}}999999}/messages'
```

### POST {{baseUrl}}/messages/{{messageName}}/

*POST message creates a message*
<sub>source: postman</sub>

**Positive — documented request** — expect `2xx`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/messages/{{messageName}}/'
```

**Negative — empty body** — expect `400 or 422`

```sh
curl -sS -X POST '{{baseUrl}}{{baseUrl}}/messages/{{messageName}}/' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**Negative — non-existent id** — expect `404`

```sh
curl -sS -X POST '{{baseUrl}}999999}/messages/999999}/'
```
